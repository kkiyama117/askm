//! Parsing and normalization for the three manifest kinds `askm` reads:
//! `SKILL.md` frontmatter, `plugin.json`, and `marketplace.json` (either dialect).

pub mod marketplace;
pub mod plugin;
pub mod skill;
